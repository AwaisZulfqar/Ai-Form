import { ArrowBigUp, MessageSquare, Share2 } from "lucide-react";

// Central icon map: components reference semantic names, not the icon library,
// so swapping icons later touches only this file.
export const UpvoteIcon = ArrowBigUp;
export const CommentIcon = MessageSquare;
export const ShareIcon = Share2;
